// Mœbius Research Atlas — minimal vanilla JS
// Future hook: load JSON data if/when fetch becomes useful. For now: static-only.
(function(){
  // nothing needed yet — placeholder for future smooth-scroll / lazy-loading enhancement
  // Reduced motion respected via CSS @media
})();
